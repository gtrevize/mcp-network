# MCP Network Server Configuration

# JWT Secret for token verification (REQUIRED in production)
JWT_SECRET=CHANGE_THIS_SECRET_IN_PRODUCTION

# Authentication Token (REQUIRED)
# Generate using: node -e "require('./dist/auth/jwt.js').generateTestToken('user-id', ['admin'])"
MCP_AUTH_TOKEN=

# Logging
LOG_LEVEL=info
NODE_ENV=production

# Access logging
ACCESS_LOG_FILE=/var/log/mcp-network/access.log

# Let's Encrypt
LETSENCRYPT_PRODUCTION=false
